const { Shapes, Circle, Square, Trinagle} = require('../lib/shapes');

describe("Shapes"), () => {
    test('should create a new Shapes class', () => {
        const shape = new Shapes("#000", "#fff", "SVG");
        
    })
}